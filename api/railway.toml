[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Railway injects PORT; the Dockerfile CMD already reads it via ${PORT:-8000}
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
